project('IO', 'c', 'cpp')

project_description = 'IO project from the Test Driven Development for Embedded C book by James W. Grenning'

include_dir = include_directories('include')

cpputest = dependency('cpputest')

subdir('src')
subdir('test')